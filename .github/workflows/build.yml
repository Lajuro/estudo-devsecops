name: Build
on:
  push:
    branches:
      - main
  pull_request:
    types: [opened, synchronize, reopened]
jobs:
  sonarqube:
    name: SonarQube
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0  # Shallow clones should be disabled for a better relevancy of analysis
      
      - name: SonarQube Scan
        uses: SonarSource/sonarqube-scan-action@v6
        with:
          args: >
            -Dsonar.qualitygate.wait=true
        env:
          SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}
          SONAR_HOST_URL: ${{ secrets.SONAR_HOST_URL }}
      
      - name: 🔍 Listar Projetos Disponíveis (Diagnóstico)
        if: always()
        uses: actions/github-script@v7
        with:
          script: |
            const http = require('http');
            const https = require('https');
            
            const sonarToken = process.env.SONAR_TOKEN;
            const sonarUrl = process.env.SONAR_HOST_URL || 'http://localhost:9000';
            
            function fetchSonarData(endpoint) {
              return new Promise((resolve, reject) => {
                try {
                  const auth = Buffer.from(`${sonarToken}:`).toString('base64');
                  const url = new URL(endpoint, sonarUrl);
                  const protocol = url.protocol === 'https:' ? https : http;
                  
                  const options = {
                    hostname: url.hostname,
                    port: url.port || (url.protocol === 'https:' ? 443 : 80),
                    path: url.pathname + url.search,
                    method: 'GET',
                    headers: {
                      'Authorization': `Basic ${auth}`,
                      'Accept': 'application/json'
                    }
                  };
                  
                  const req = protocol.request(options, (res) => {
                    let data = '';
                    res.on('data', chunk => data += chunk);
                    res.on('end', () => {
                      try {
                        resolve(JSON.parse(data));
                      } catch (e) {
                        reject(e);
                      }
                    });
                  });
                  
                  req.on('error', reject);
                  req.end();
                } catch (e) {
                  reject(e);
                }
              });
            }
            
            try {
              console.log(`📋 Listando projetos disponíveis no SonarQube...`);
              const projects = await fetchSonarData(`/api/projects/search?ps=100`);
              
              if (projects.components && projects.components.length > 0) {
                console.log(`\n✅ ${projects.components.length} projeto(s) encontrado(s):\n`);
                projects.components.forEach((proj, idx) => {
                  console.log(`${idx + 1}. ${proj.name}`);
                  console.log(`   📍 Key: ${proj.key}`);
                  console.log(`   📊 LastAnalysis: ${proj.lastAnalysisDate || 'Nunca'}\n`);
                });
                
                console.log(`\n💡 Use o "Key" no seu workflow:`);
                console.log(`   SONAR_PROJECT_KEY: ${projects.components[0].key}`);
              } else {
                console.log(`⚠️ Nenhum projeto encontrado no SonarQube`);
                console.log(`Certifique-se de que uma análise foi executada primeiro`);
              }
            } catch (error) {
              console.error(`❌ Erro ao listar projetos:`, error.message);
            }
        env:
          SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}
          SONAR_HOST_URL: ${{ secrets.SONAR_HOST_URL }}
      
      - name: 📊 Gerar Relatório SonarQube Detalhado
        if: always()
        uses: actions/github-script@v7
        with:
          script: |
            const fs = require('fs');
            const http = require('http');
            const https = require('https');
            
            // Configurações
            const sonarToken = process.env.SONAR_TOKEN;
            const sonarUrl = process.env.SONAR_HOST_URL || 'http://localhost:9000';
            let projectKey = process.env.SONAR_PROJECT_KEY;
            
            function fetchSonarData(endpoint) {
              return new Promise((resolve, reject) => {
                try {
                  const auth = Buffer.from(`${sonarToken}:`).toString('base64');
                  const url = new URL(endpoint, sonarUrl);
                  const protocol = url.protocol === 'https:' ? https : http;
                  
                  const options = {
                    hostname: url.hostname,
                    port: url.port || (url.protocol === 'https:' ? 443 : 80),
                    path: url.pathname + url.search,
                    method: 'GET',
                    headers: {
                      'Authorization': `Basic ${auth}`,
                      'Accept': 'application/json'
                    }
                  };
                  
                  const req = protocol.request(options, (res) => {
                    let data = '';
                    res.on('data', chunk => data += chunk);
                    res.on('end', () => {
                      try {
                        const json = JSON.parse(data);
                        resolve(json);
                      } catch (e) {
                        reject(e);
                      }
                    });
                  });
                  
                  req.on('error', reject);
                  req.end();
                } catch (e) {
                  reject(e);
                }
              });
            }
            
            try {
              console.log(`🔍 Conectando ao SonarQube: ${sonarUrl}`);
              
              // Se não tiver project key, tenta pegar o primeiro projeto
              if (!projectKey) {
                console.log(`⚠️ SONAR_PROJECT_KEY não definido. Tentando descobrir automaticamente...`);
                const projects = await fetchSonarData(`/api/projects/search?ps=100`);
                
                if (!projects.components || projects.components.length === 0) {
                  throw new Error(`Nenhum projeto encontrado no SonarQube. Execute uma análise primeiro.`);
                }
                
                projectKey = projects.components[0].key;
                console.log(`📦 Projeto encontrado: ${projectKey}`);
              } else {
                console.log(`📦 Projeto: ${projectKey}`);
              }
              
              // Buscar informações do projeto
              console.log(`📊 Buscando métricas do projeto...`);
              const projectMetrics = await fetchSonarData(`/api/measures/component?component=${projectKey}&metricKeys=bugs,vulnerabilities,code_smells,coverage,duplicated_lines_density,security_rating,reliability_rating,maintainability_rating`);
              
              if (!projectMetrics.component) {
                throw new Error(`Projeto "${projectKey}" não encontrado. Verifique o SONAR_PROJECT_KEY.`);
              }
              
              const measures = (projectMetrics.component.measures || []).reduce((acc, m) => {
                acc[m.metric] = m.value;
                return acc;
              }, {});
              
              // Buscar issues
              console.log(`🔍 Buscando issues do projeto...`);
              const issuesResponse = await fetchSonarData(`/api/issues/search?componentKeys=${projectKey}&types=BUG,VULNERABILITY,CODE_SMELL&ps=100`);
              
              const issues = issuesResponse.issues || [];
              console.log(`✅ ${issues.length} issues encontradas`);
              
              // Contar issues por tipo
              const bugCount = issues.filter(i => i.type === 'BUG').length;
              const vulnCount = issues.filter(i => i.type === 'VULNERABILITY').length;
              const smellCount = issues.filter(i => i.type === 'CODE_SMELL').length;
              
              // Contar por severidade
              const severities = issues.reduce((acc, i) => {
                acc[i.severity] = (acc[i.severity] || 0) + 1;
                return acc;
              }, {});
              
              // Gerar markdown report
              let report = `# 📋 Relatório de Análise SonarQube\n\n`;
              report += `**Data:** ${new Date().toLocaleString('pt-BR')}\n`;
              report += `**Projeto:** ${projectKey}\n\n`;
              
              // Status geral
              report += `## 📊 Métricas Gerais\n\n`;
              report += `| Métrica | Valor |\n`;
              report += `|---------|-------|\n`;
              report += `| 🐛 Bugs | ${measures.bugs || '0'} |\n`;
              report += `| 🔒 Vulnerabilidades | ${measures.vulnerabilities || '0'} |\n`;
              report += `| 💡 Code Smells | ${measures.code_smells || '0'} |\n`;
              report += `| 📈 Cobertura de Testes | ${measures.coverage || 'N/A'} |\n`;
              report += `| 🔁 Linhas Duplicadas | ${measures.duplicated_lines_density || 'N/A'} |\n`;
              report += `| ⭐ Rating de Segurança | ${measures.security_rating || 'N/A'} |\n`;
              report += `| 🎯 Rating de Confiabilidade | ${measures.reliability_rating || 'N/A'} |\n`;
              report += `| 🧹 Rating de Manutenibilidade | ${measures.maintainability_rating || 'N/A'} |\n\n`;
              
              // Resumo de issues
              report += `## 🔍 Resumo de Issues\n\n`;
              report += `- **Total de Issues:** ${issues.length}\n`;
              report += `- **Bugs:** ${bugCount}\n`;
              report += `- **Vulnerabilidades:** ${vulnCount}\n`;
              report += `- **Code Smells:** ${smellCount}\n\n`;
              
              if (Object.keys(severities).length > 0) {
                report += `### Por Severidade\n`;
                report += `| Severidade | Quantidade |\n`;
                report += `|------------|------------|\n`;
                Object.entries(severities).forEach(([sev, count]) => {
                  const icon = sev === 'BLOCKER' ? '🔴' : sev === 'CRITICAL' ? '🟠' : sev === 'MAJOR' ? '🟡' : sev === 'MINOR' ? '🔵' : '⚪';
                  report += `| ${icon} ${sev} | ${count} |\n`;
                });
                report += '\n';
              }
              
              // Top 10 issues mais graves
              if (issues.length > 0) {
                report += `## ⚠️ Issues Mais Graves\n\n`;
                const sorted = issues
                  .sort((a, b) => {
                    const severityOrder = { BLOCKER: 0, CRITICAL: 1, MAJOR: 2, MINOR: 3, INFO: 4 };
                    return severityOrder[a.severity] - severityOrder[b.severity];
                  })
                  .slice(0, 10);
                
                sorted.forEach((issue, idx) => {
                  const icon = issue.severity === 'BLOCKER' ? '🔴' : 
                               issue.severity === 'CRITICAL' ? '🟠' : 
                               issue.severity === 'MAJOR' ? '🟡' : 
                               issue.severity === 'MINOR' ? '🔵' : '⚪';
                  const typeIcon = issue.type === 'BUG' ? '🐛' : 
                                   issue.type === 'VULNERABILITY' ? '🔒' : '💡';
                  
                  const fileName = issue.component ? issue.component.split(':').pop() : 'Desconhecido';
                  
                  report += `\n### ${idx + 1}. ${icon} ${typeIcon} ${issue.message}\n`;
                  report += `- **Arquivo:** \`${fileName}\`\n`;
                  if (issue.line) report += `- **Linha:** ${issue.line}\n`;
                  report += `- **Severidade:** ${issue.severity}\n`;
                  report += `- **Status:** ${issue.status}\n`;
                });
                report += '\n';
              } else {
                report += `## ✅ Nenhuma Issue Encontrada\n\n`;
                report += `Excelente! Seu código não possui bugs, vulnerabilidades ou code smells críticos.\n\n`;
              }
              
              // Links úteis
              report += `## 🔗 Links Úteis\n\n`;
              report += `- [🔗 Abrir no SonarQube](${sonarUrl}/dashboard?id=${projectKey})\n`;
              report += `- [📝 Issues Completas](${sonarUrl}/issues?id=${projectKey})\n`;
              report += `- [📊 Métricas Detalhadas](${sonarUrl}/component_measures?id=${projectKey})\n\n`;
              
              // Recomendações
              report += `## 💡 Recomendações\n\n`;
              if (vulnCount > 0) {
                report += `⚠️ **Prioridade Alta:** Existem ${vulnCount} vulnerabilidade(s) de segurança que precisam ser corrigidas imediatamente.\n\n`;
              }
              if (bugCount > 0) {
                report += `🔧 **Ação Necessária:** ${bugCount} bug(s) detectado(s). Revisar e corrigir é essencial.\n\n`;
              }
              if (smellCount > 0) {
                report += `✨ **Melhorias:** ${smellCount} code smell(s) encontrado(s). Considere refatorar para melhor qualidade do código.\n\n`;
              }
              if (vulnCount === 0 && bugCount === 0 && smellCount === 0) {
                report += `✨ **Parabéns!** Seu código está em excelente estado.\n\n`;
              }
              
              // Escrever no arquivo de summary
              fs.appendFileSync(process.env.GITHUB_STEP_SUMMARY, report);
              
              console.log('✅ Relatório gerado com sucesso!');
            } catch (error) {
              console.error('❌ Erro ao gerar relatório:', error.message);
              
              // Escrever um relatório de erro
              let errorReport = `# ❌ Erro ao Gerar Relatório SonarQube\n\n`;
              errorReport += `**Erro:** ${error.message}\n\n`;
              errorReport += `### 🔧 Verificações Necessárias\n\n`;
              errorReport += `1. ✓ Certifique-se que \`SONAR_TOKEN\` está configurado nos secrets\n`;
              errorReport += `2. ✓ Verifique se \`SONAR_HOST_URL\` está configurado (ex: \`http://localhost:9000\`)\n`;
              errorReport += `3. ✓ Confirme que a análise SonarQube foi executada corretamente\n`;
              errorReport += `4. ✓ Aguarde alguns minutos após a primeira análise para os dados ficarem disponíveis\n\n`;
              errorReport += `**URL do SonarQube:** ${sonarUrl}\n`;
              errorReport += `**Token:** ${sonarToken ? '✓ Configurado' : '✗ Não configurado'}\n`;
              errorReport += `**Project Key:** ${projectKey || 'Auto-detectar'}\n`;
              
              fs.appendFileSync(process.env.GITHUB_STEP_SUMMARY, errorReport);
            }
        env:
          SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}
          SONAR_HOST_URL: ${{ secrets.SONAR_HOST_URL }}